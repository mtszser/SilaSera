<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SilaSera" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/darkBlue</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/black</item>
        <!-- Customize your theme here. -->

        <!-- This is to enable transition. -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- Setting up enter and exit transition. -->
        <item name="android:windowEnterTransition">@transition/trans_anim</item>
        <item name="android:fragmentExitTransition">@transition/trans_anim</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.SeraSila" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/gradient</item>
        <item name="colorOnPrimary">@color/darkBlue</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/gradient</item>
        <!-- Customize your theme here. -->

        <!-- This is to enable transition. -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- Setting up enter and exit transition. -->
        <item name="android:windowEnterTransition">@transition/trans_anim</item>
        <item name="android:fragmentExitTransition">@transition/trans_anim</item>
    </style>

    <style name="Theme.ActionBar" parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cheeseYellow</item>
        <item name="colorPrimaryVariant">@color/darkerCheeseYellow</item>
        <item name="colorOnPrimary">@color/yellow</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/cheeseYellow</item>
        <!-- Customize your theme here. -->

        <!-- This is to enable transition. -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- Setting up enter and exit transition. -->
        <item name="android:windowEnterTransition">@transition/trans_anim</item>
        <item name="android:fragmentExitTransition">@transition/trans_anim</item>
    </style>

    <style name="Theme.SplashScreen1" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:windowActionBarOverlay">true</item>
        <item name="colorPrimaryVariant">#EFD683</item>
        <item name="colorOnPrimary">@color/cheeseYellow</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/cheeseYellow</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">#F1B51B</item>
        <item name="android:navigationBarColor">@color/gradient</item>

        <!-- Customize your theme here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

        <!-- This is to enable transition. -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- Setting up enter and exit transition. -->
        <item name="android:windowEnterTransition">@transition/trans_anim</item>
        <item name="android:fragmentExitTransition">@transition/trans_anim</item>
    </style>

    <style name="Theme.TopBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cheeseYellow</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/cheeseYellow</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/cheeseYellow</item>
        <!-- Customize your theme here. -->

        <!-- This is to enable transition. -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- Setting up enter and exit transition. -->
        <item name="android:windowEnterTransition">@transition/trans_anim</item>
        <item name="android:fragmentExitTransition">@transition/trans_anim</item>
    </style>

    <style name="Theme.AppMain" parent="Theme.AppCompat.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cheeseYellow</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/darkBlue</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/cheeseYellow</item>
        <!-- Customize your theme here. -->

        <!-- This is to enable transition. -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- Setting up enter and exit transition. -->
        <item name="android:windowEnterTransition">@transition/trans_anim</item>
        <item name="android:fragmentExitTransition">@transition/trans_anim</item>
    </style>

    <style name="ThemeOverlay.AppCompat.navTheme">

        <!-- Color of text and icon when SELECTED -->
        <item name="colorPrimary">@color/black</item>

        <!-- Background color when SELECTED -->
        <item name="colorControlHighlight">#4D000000</item>

    </style>


</resources>